void setup(){
  size(550, 550);
  background(255);
  noLoop();
}
void draw(){
  

  int y=0;
  while (y<506){
    int x=0;
    while (x<506){
      stroke(0);
      rectMode(CENTER);
      fill(255);
       float rand= random(0,50);
  if (rand<35) {
      rect(25+x, 25+y, 15, 15);
  }
     x+=25;
    }
    y+=25;
  }
}
